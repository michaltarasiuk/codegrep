{
  "extends": ["../../tsconfig.json"],
  "compilerOptions": {
    "baseUrl": ".",
    "moduleDetection": "force",
    "moduleResolution": "node",
    "paths": {
      "$api/*": ["./src/*"]
    },
    "types": ["bun-types"]
  }
}
