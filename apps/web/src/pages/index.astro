---
import { client } from "$lib/utils/client";

export const prerender = false;

const chat = await client.api.chat.create.post(
  {},
  {
    headers: Object.fromEntries(Astro.request.headers),
  }
);
if (!chat.error) {
  return Astro.redirect(`/chat/${chat.data.id}`);
}
---
