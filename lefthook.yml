pre-commit:
  jobs:
    - run: bun x prettier --write --ignore-unknown {staged_files}
    - run: bun x eslint --no-warn-ignored {staged_files}
      glob: "*.{js,mjs,cjs,ts,mts,cts,svelte,astro}"
    - run: bun x tsc --noEmit --pretty false {staged_files}
      glob: "*.{ts,tsx}"
